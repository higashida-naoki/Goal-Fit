<main>
  <div class="top-page">
    <div class="image-container">
      <img src="<%= asset_path('woman running.webp') %>" alt="Forest Image" class="background-image">
      <div class="image-text">
        <h1>理想の体(GOAL)を追求(FIT)する<br><br><strong>〜 GOAL FIT 〜</strong></h1>
      </div>
      <div class="button-container">
        <%= link_to '新規登録', new_user_registration_path, class: 'btn' %>
        <%= link_to 'ログイン', new_user_session_path, class: 'btn' %>
        <%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, method: :post, class: 'btn' %>
      </div>
    </div>
  </div>
</main>

<style>
  /* TOPページのスタイル */
  .top-page {
    display: flex;
    align-items: center;
    justify-content: center;
    height: 100vh; /* ビューポートの高さを適用 */
    position: relative;
    text-align: center;
    flex-direction: column;
    overflow: hidden; /* はみ出し防止 */
  }

  .image-container {
    position: absolute;
    width: 80%;
    height: 100%;
    overflow: hidden;
  }

  .background-image {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover; /* 縦横比を維持しつつ、画面全体にフィット */
    border-top: 30px solid black; /* 画像の上端に黒の枠線を追加 */
    border-bottom: 30px solid black; /* 画像の下端に黒の枠線を追加 */
  }

  .image-text {
    position: absolute;
    top: 40%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: white;
    font-size: 2.5rem; /* 全体のフォントサイズを小さくする */
    font-weight: bold;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6); /* 文字に影をつける */
    max-wigth: 80%;
    white-space: nowrap
}

.image-text strong {
    font-size: 3rem; /* 強調部分のフォントサイズを元のサイズに戻す */
}

@media (max-width: 768px) {
    .image-text {
        font-size: 1.3rem; /* モバイル用のフォントサイズを調整 */
    }
    .image-text strong {
        font-size: 1.5rem; /* モバイル用の強調部分のフォントサイズを調整 */
    }
    .btn {
        font-size: 0.9rem; /* モバイル用にボタンの文字サイズを小さく */
        padding: 10px 20px; /* ボタンの内側の余白を小さく */
    }
}


  /* ボタンのスタイル */
  .button-container {
    position: absolute;
    bottom: 150px; /* 画像の下部に配置 */
    left: 50%;
    transform: translateX(-50%); /* 中央寄せ */
    display: flex;
    gap: 20px;
  }

  .btn {
    background-color: white;
    color: black;
    padding: 12px 24px;
    border-radius: 50px; /* 楕円形にする */
    text-decoration: none;
    font-size: 1rem;
    font-weight: bold;
    text-align: center;
    transition: background-color 0.3s;
  }

  .btn:hover {
    background-color: #f0f0f0; /* ホバー時の背景色 */
  }

  @media (max-width: 768px) {
    .image-text {
      font-size: 1.5rem;
    }
    .btn {
      font-size: 0.9rem; /* モバイル用にボタンの文字サイズを小さく */
      padding: 10px 20px; /* ボタンの内側の余白を小さく */
    }
  }
</style>
