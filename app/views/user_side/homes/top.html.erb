<main>
  <div class="top-page">
    <div class="image-container">
      <img src="<%= asset_path('woman running.webp') %>" alt="Forest Image" class="background-image">
      <div class="image-text">
        <h1>理想の体(GOAL)を追求(FIT)する<br><br><strong>〜 GOAL FIT 〜</strong></h1>
      </div>
      <div class="button-container">
        <%= link_to '新規登録', new_user_registration_path, class: 'btn' %>
        <%= link_to 'ログイン', new_user_session_path, class: 'btn' %>
        <%= link_to 'ゲストログイン（閲覧用）', users_guest_sign_in_path, method: :post, class: 'btn' %>
      </div>
    </div>
  </div>
</main>

<style>
  /* TOPページのスタイル */
  .top-page {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  position: relative;
  text-align: center;
  overflow: hidden;
}

.image-container {
  position: absolute;
  width: 100%;
  height: 100%;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.background-image {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-top: 30px solid black;
  border-bottom: 30px solid black;
  z-index: -1; /* 背景として他要素の下に表示 */
}

.image-text {
  color: white;
  font-size: 2.5rem;
  font-weight: bold;
  text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.6);
  max-width: 90%;
  padding: 10px;
}

.image-text strong {
  font-size: 3rem;
}

.button-container {
  margin-top: 40px;
  display: flex;
  gap: 20px;
  flex-wrap: wrap;
  justify-content: center;
}

.btn {
  background-color: white;
  color: black;
  padding: 12px 24px;
  border-radius: 50px;
  text-decoration: none;
  font-size: 1rem;
  font-weight: bold;
  text-align: center;
  transition: background-color 0.3s, box-shadow 0.3s, transform 0.2s;
  box-shadow: 0 5px 10px rgba(0,0,0,0.3);
}

.btn:hover {
  background-color: #f0f0f0;
  box-shadow: 0 8px 15px rgba(0,0,0,0.5);
}

.btn:active {
  transform: translateY(2px);
  box-shadow: 0 2px 5px rgba(0,0,0,0.2);
}

/* 📱 モバイル対応 */
@media (max-width: 768px) {
  .image-text {
    font-size: 1.3rem;
    padding: 5px;
  }

  .image-text strong {
    font-size: 1.5rem;
  }

  .btn {
    font-size: 0.9rem;
    padding: 10px 16px;
  }

  .button-container {
    flex-direction: column;
    gap: 10px;
    margin-top: 30px;
  }
}

</style>
