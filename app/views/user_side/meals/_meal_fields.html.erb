<div class="nested-fields">
  <%= f.collection_select :meal_type_id, MealType.all, :id, :name %>
  <%= f.text_field :content, placeholder: "内容" %>
  <%= f.number_field :calories, placeholder: "カロリー" %>
  <%= link_to_remove_association "削除", f %>
</div>
